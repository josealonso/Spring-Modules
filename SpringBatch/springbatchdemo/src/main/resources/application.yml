spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/javatechie
    username: root
    password: Password
  jpa.show-sql: true
  jpa.hibernate.ddl-auto: update
  jpa.properties.hibernate.dialect: org.hibernate.dialect.MySQL5Dialect

  batch:
    initialize-schema: ALWAYS
    job.enabled: false  # disabled job run at startup

server:
  port=9191


